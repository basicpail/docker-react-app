branches:
  only:
    - aws

sudo: required

language: generic

services:
  - docker

before_install:
  - echo "Start creating an image with dockerfile on Travis!"
  - docker build -t basicpail93/docker-react-app -f Dockerfile .
  # - docker build -t basicpail93/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true basicpail93/docker-react-app npm run test -- --coverage

after_success:
  - echo "build success!"

deploy:
  edge: true
  provider: elasticbeanstalk
  region: ap-northeast-2
  app: docker-react-app
  env: Docker-react-app-env-1
  bucket_name: elasticbeanstalk-ap-northeast-2-242201304740
  bucket_path: docker-react-app
  on:
    branch: aws
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_ACCESS_KEY